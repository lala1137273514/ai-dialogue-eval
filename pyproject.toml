[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-dialogue-eval"
version = "3.0.0"
description = "AI 对话评测系统 Pro"
requires-python = ">=3.8"
dependencies = [
    "streamlit>=1.28.0",
    "pandas>=2.0.0",
    "plotly>=5.0.0",
    "openai>=1.0.0",
    "pyyaml>=6.0"
]
