# AI 对话评测系统 - 项目思考与演进历程

> 作为一名刚入行的 AI 产品经理，我在这个项目中遇到了什么问题、如何思考解决方案、以及对未来的规划

---

## 一、项目缘起：我遇到了什么问题

### 1.1 痛点发现

作为 AI 产品经理，我负责一个医美咨询场景的 AI 客服项目。上线后，我发现一个棘手的问题：

> **"AI 客服回复的质量参差不齐，但我无法系统性地评估和改进。"**

具体表现：
- 有些回复用户觉得很好，有些却投诉"答非所问"
- 运营同事反馈某些回复"太生硬"，但缺乏量化标准
- 我想改进 Prompt，但不知道具体哪里有问题

### 1.2 现有方案的局限

我调研了现有的评测方式，发现都不太理想：

| 方案 | 问题 |
|------|------|
| **人工质检** | 成本高、效率低、标准不统一，每天只能抽检几十条 |
| **规则匹配** | 只能检测关键词，无法理解语义，容易误判 |
| **传统 NLP** | 难以评估"共情"、"权威感"这类软性指标 |
| **用户反馈** | 滞后、样本偏差大，投诉的只是冰山一角 |

### 1.3 核心需求提炼

我梳理了自己的核心需求：

1. **自动化**：能批量评测对话日志，不依赖人工
2. **多维度**：不止"好/坏"，要细分维度（清晰度、共情度、准确性等）
3. **可追溯**：不止告诉我"哪里错了"，还要告诉我"为什么错"和"怎么改"
4. **效率高**：评测速度要快，Token 成本可控

---

## 二、解决思路：我是如何思考的

### 2.1 灵感来源：LLM-as-a-Judge

在阅读 AI 评测相关论文时，我了解到 **LLM-as-a-Judge** 这个范式：

> 用大语言模型作为评测裁判，对另一个 AI 的输出进行评分和分析。

这让我眼前一亮：
- LLM 能理解语义，不是简单的规则匹配
- LLM 能评估"软性"指标，比如语气、共情、权威感
- 通过 Prompt 定义评分标准，灵活可调

### 2.2 我的设计思路

#### 2.2.1 先确定评什么（评分维度）

我参考了客服质检的行业标准，结合医美咨询场景，定义了 6 个核心维度：

| 维度 | 为什么重要 |
|------|-----------|
| **表达清晰度** | 用户能否快速理解回复内容 |
| **主动引导能力** | AI 是否能挖掘用户需求，推进对话 |
| **内容价值呈现** | 是否传递了有用信息和产品优势 |
| **专业权威感** | 用户是否信任 AI 的专业能力 |
| **信息准确性** | 是否有事实错误或虚假承诺 |
| **语气共情度** | 是否让用户感到被理解和尊重 |

#### 2.2.2 再确定怎么评（评测流程）

我设计了两阶段评测流程：

**Phase 1：快速打分**
- 让 LLM 对每条 AI 回复按 6 个维度打 1-5 分
- 同时输出简短的综合分析

**Phase 2：深度分析（仅低分触发）**
- 低分回复单独进行根因分析
- 结合工作流配置，溯源到具体节点
- 给出可执行的 Prompt 修改建议

#### 2.2.3 最后解决效率问题

最初的设计是每个维度单独调用 LLM，导致：
- 一条回复需要 6 次 LLM 调用
- 评测速度慢，Token 消耗大

后来我优化为 **合并评测**：
- 一次调用同时输出 6 个维度的分数
- 效率提升 6 倍

### 2.3 关键设计决策

#### 决策 1：综合分如何计算？

**问题**：如果某个维度得 1 分（比如信息准确性有虚假承诺），但其他 5 个维度都是 5 分，平均分是 4.33——看起来"良好"，但实际上有严重问题。

**我的思考**：
- 单纯平均分会掩盖短板
- 某些维度的低分是"一票否决"的（比如虚假承诺）

**解决方案**：最低分惩罚机制
```
综合分 = min(平均分, 最低分 + 1.5)
```

这样，上面的例子综合分变成 2.5，会触发深度分析。

#### 决策 2：如何做到"可追溯"？

**问题**：知道某条回复有问题，但不知道是哪个 Prompt 导致的。

**我的思考**：
- 我们的 AI 客服是用 Dify 搭建的工作流
- 工作流里有多个 LLM 节点，每个节点有不同的 Prompt
- 如果能把低分回复追溯到具体节点，就知道该改哪里

**解决方案**：工作流节点溯源
1. 解析 Dify 导出的 YML 配置
2. 提取所有 LLM 节点的 Prompt
3. 在深度分析时，让 LLM 判断回复最可能是哪个节点生成的
4. 给出针对该节点 Prompt 的修改建议

#### 决策 3：评测结果如何管理？

**问题**：每次评测完，数据就没了，无法查看历史记录。

**我的思考**：
- 需要持久化存储
- 要能追溯"上周的评测结果是什么"
- 最好能对比不同批次的变化

**解决方案**：SQLite 数据库存储
- 评测完成后自动保存
- 历史记录可查询、可删除
- 记录关联的日志文件、工作流配置

---

## 三、功能实现：这个系统解决了什么

### 3.1 核心功能价值

| 功能 | 解决的问题 |
|------|-----------|
| **合并评测** | 效率提升 6 倍，Token 成本降低 80% |
| **综合分算法** | 不遗漏任何维度的严重问题 |
| **深度分析** | 从"发现问题"到"知道怎么改" |
| **节点溯源** | 直接告诉你该改哪个 Prompt |
| **历史评测** | 追溯历史，对比改进效果 |
| **可视化仪表盘** | 一目了然地看到整体质量分布 |

### 3.2 实际使用场景

#### 场景 1：日常质量监控
- 每天/每周跑一次评测
- 查看平均分趋势，发现质量波动
- 重点关注低分回复

#### 场景 2：Prompt 优化
- 评测发现某维度低分较多
- 通过节点溯源找到问题 Prompt
- 根据修改建议优化
- 重新评测验证效果

#### 场景 3：新场景上线前验证
- 用测试对话做评测
- 确保各维度都达标
- 修复问题后再上线

---

## 四、迭代历程：项目是如何演进的

### v1.0：能跑起来

**目标**：验证 LLM-as-a-Judge 的可行性

**做了什么**：
- 加载对话日志和评分标准
- 调用 LLM 逐维度打分
- 简单的表格展示结果

**问题**：
- 每条回复 6 次调用，太慢了
- 只有分数，没有分析，不知道怎么改

### v2.0：能用了

**目标**：增加深度分析，支持问题诊断

**做了什么**：
- 两阶段评测：快速打分 + 低分深度分析
- 工作流 YML 解析，支持节点溯源
- 日志回放页面，对话与评分联动
- 报告导出（Markdown / JSON）

**问题**：
- 评测仍需 6 次调用/回复
- 平均分可能掩盖严重问题

### v3.0：好用了（当前版本）

**目标**：效率优化 + 评分精准 + 数据持久化

**做了什么**：
- **合并评测**：1 次调用 = 6 维度分数，效率提升 6 倍
- **综合分算法**：最低分惩罚机制
- **SQLite 存储**：历史评测可追溯
- **历史评测页面**：增删改查
- **低分阈值可调**：1/2/3/4 可选

---

## 五、后续规划：还有哪些可以优化

### 5.1 短期优化（1-2 周）

#### 5.1.1 评测体系优化

- [ ] **维度权重可配置**：不同场景可能需要不同的权重，比如医美咨询更看重"信息准确性"
- [ ] **自定义维度**：除了预设的 6 个维度，支持用户添加业务特定的维度
- [ ] **检查清单细化**：每个维度的低分检查项需要更具体，便于 LLM 准确识别

#### 5.1.2 评测标准迭代

当前的 rubric.json 还比较粗糙，需要：
- 根据实际评测结果调整各档位描述
- 收集典型案例，形成参考样本
- 区分不同业务场景的评分标准

### 5.2 中期规划（1-2 个月）

#### 5.2.1 功能增强

- [ ] **评测对比**：对比不同批次的评测结果，验证 Prompt 优化效果
- [ ] **趋势分析**：时间维度的质量变化曲线
- [ ] **多模型评测**：用 GPT / Claude / 其他模型分别评测，交叉验证
- [ ] **批量 Prompt 优化**：根据低分分析，自动生成优化后的 Prompt 供参考

#### 5.2.2 工程优化

- [ ] **并行评测**：多线程/异步调用，进一步提升速度
- [ ] **API 接口**：将评测能力封装为 API，支持集成到 CI/CD
- [ ] **定时任务**：支持定时自动评测

### 5.3 长期愿景（3-6 个月）

#### 5.3.1 评测标准体系化

- 总结不同场景的评测标准模板
- 形成行业级的对话质量评测规范
- 建立评测案例库

#### 5.3.2 闭环优化

- 评测 → 分析 → 优化 → 验证的完整闭环
- 自动生成训练数据，用于模型微调
- 评测结果与用户反馈关联，验证评测准确性

#### 5.3.3 产品化

- 支持多租户
- 权限管理
- 可视化报表定制

---

## 六、反思与收获

### 6.1 我学到了什么

1. **从用户痛点出发**：不是先想功能，而是先想"用户遇到了什么问题"
2. **迭代思维**：v1 能跑，v2 能用，v3 好用，一步步来
3. **权衡取舍**：合并评测牺牲了一点 Prompt 简洁性，但换来了 6 倍效率提升
4. **系统思维**：评测不是孤立的，需要和工作流、历史记录、报告导出形成体系

### 6.2 还有哪些不足

1. **评分标准主观性**：不同人对"4 分"和"3 分"的理解可能不同
2. **LLM 评测的偏差**：LLM 可能有自己的偏好，需要更多验证
3. **场景覆盖**：目前主要针对咨询场景，其他场景可能需要调整

### 6.3 未来的思考

- 如何让评测结果更客观？→ 多模型交叉验证 + 人工抽样校准
- 如何让评测标准持续优化？→ 建立反馈机制，定期迭代
- 如何让系统更智能？→ 自动发现问题模式，主动推荐优化方向

---

## 七、对话历程摘要

### 关键节点

| 时间 | 话题 | 产出 |
|------|------|------|
| 阶段 1 | 项目初始化 | 基础评测框架，6 维度打分 |
| 阶段 2 | 两阶段评测 | 深度分析 + 节点溯源 |
| 阶段 3 | 日志回放增强 | 三栏布局，评分与对话联动 |
| 阶段 4 | 报告导出 | Markdown/JSON 完整报告 |
| 阶段 5 | 评测逻辑优化 | 合并评测，效率提升 6 倍 |
| 阶段 6 | 综合分算法 | 最低分惩罚机制 |
| 阶段 7 | SQLite 接入 | 历史评测持久化 |
| 阶段 8 | 历史评测页面 | 增删改查功能 |
| 阶段 9 | 文档更新 | README/rule/thought/项目报告 |

### 主要决策点

1. **为什么用 LLM 评测？** → 能理解语义，能评估软性指标
2. **为什么要合并评测？** → 效率优先，6 次调用优化为 1 次
3. **为什么需要综合分惩罚？** → 防止严重问题被平均分掩盖
4. **为什么做节点溯源？** → 从"发现问题"到"知道怎么改"
5. **为什么用 SQLite？** → 单文件、零配置、满足持久化需求

---

*AI 对话评测系统 Pro v3.0 | 项目思考文档 | 2024-12-18*

*作者：AI 产品经理（刚入行）*
