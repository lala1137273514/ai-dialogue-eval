# AI 对话评测系统 Pro - 项目报告

> 完整的功能介绍、评测思路、评测指标与数据格式要求

---

## 一、项目概述

### 1.1 项目定位

**AI 对话评测系统 Pro** 是一个基于 LLM-as-a-Judge 的多轮对话质量评测平台，专注于：

- 🎯 **客服/咨询对话质量评估**
- 🔍 **问题诊断与根因分析**
- 📊 **工作流节点溯源与优化建议**

### 1.2 核心价值

| 维度 | 价值 |
|------|------|
| **效率** | 合并评测，每条回复 1 次调用，效率提升 6 倍 |
| **精准** | 最低分惩罚机制，不遗漏严重问题 |
| **深度** | 低分回复自动根因分析 + 节点溯源 |
| **可追溯** | SQLite 持久化，历史记录可追溯 |

---

## 二、功能介绍

### 2.1 系统架构

```
┌──────────────────────────────────────────────────────────────┐
│                     Streamlit 前端                           │
├──────────────────────────────────────────────────────────────┤
│  工作台 │ 日志回放 │ 智能评测 │ 低分分析 │ 历史评测 │ 配置  │
├──────────────────────────────────────────────────────────────┤
│                     评测引擎 (run_eval.py)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ Phase 1    │→│ 综合分计算  │→│ Phase 2    │          │
│  │ 快速评测   │  │ 最低分惩罚  │  │ 深度分析   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
├──────────────────────────────────────────────────────────────┤
│  数据层：SQLite + JSON 文件                                   │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 功能模块详解

#### 📊 工作台
- 数据源加载状态显示
- 快捷操作入口
- 系统健康状态概览

#### 📜 日志回放
- **三栏布局**：会话列表 | 对话内容 | 评测结果
- **评分联动**：点击消息旁评分按钮，右侧显示详情
- **颜色编码**：🟢绿(≥4) / 🟡黄(3) / 🔴红(≤2)

#### 🚀 智能评测
- **Phase 1**：合并快速评测
  - 1 次 LLM 调用 = 6 维度分数 + 综合分析
- **Phase 2**：低分深度分析
  - 根因分析 + 节点溯源 + 修改建议
- **可配置项**：低分阈值 1/2/3/4
- **自动保存**：结果存入 SQLite

#### 🔍 低分分析
- 问题回复聚合展示
- 根因分析详情
- 工作流节点溯源
- Prompt 修改建议

#### 📚 历史评测
- **统计卡片**：批次数/评测数/低分数/平均分
- **批次列表**：按时间倒序
- **关联文件**：日志/工作流/评分标准
- **详情展开**：会话列表 + Turn 评分表
- **删除功能**：二次确认删除

#### 🛠️ 评分标准配置
- 在线编辑 rubric.json
- 实时语法校验

#### 💡 Prompt 工坊
- Prompt 生成与优化
- 流式输出

---

## 三、评测思路

### 3.1 评测范式：LLM-as-a-Judge

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  对话日志      │ + │  评分标准       │ → │  LLM 评测裁判  │
│  (待评测内容)   │    │  (rubric.json)   │    │  (打分+分析)    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 3.2 两阶段评测流程

#### Phase 1：快速评测
- **输入**：对话历史 + 待评测回复 + 评分标准
- **输出**：6 维度分数 + 平均分 + 综合分析
- **效率**：1 次 LLM 调用/回复

#### Phase 2：深度分析
- **触发条件**：综合分 ≤ 阈值 OR 任意维度 ≤ (阈值-1)
- **输出**：根因分析 + 节点溯源 + 修改建议
- **按需触发**：仅低分回复执行

### 3.3 综合分算法

```python
综合分 = min(平均分, 最低分 + 1.5)
```

**设计原理**：
- 单纯平均分会掩盖某维度的严重问题
- 最低分惩罚确保重大缺陷被识别

**示例**：

| 各维度分数 | 平均分 | 最低分 | 综合分 | 是否触发分析 |
|-----------|--------|--------|--------|-------------|
| 4,4,4,4,4,4 | 4.0 | 4 | 4.0 | 否 |
| 1,5,5,5,5,5 | 4.33 | 1 | **2.5** | **是** |
| 2,2,4,4,5,5 | 3.67 | 2 | **3.5** | **是** |

---

## 四、评测指标

### 4.1 六维度评分体系

| 维度代码 | 中文名称 | 评估重点 | 权重 |
|----------|----------|----------|------|
| `clarity_sentence_structure` | 表达清晰度 | 语句通顺、结构清晰、逻辑连贯 | 均等 |
| `proactivity_interaction` | 主动引导能力 | 主动提问、需求挖掘、对话推进 | 均等 |
| `content_benefits` | 内容价值呈现 | 信息量、价值传递、利益点突出 | 均等 |
| `persona_authority` | 专业权威感 | 专业术语、权威感、信任建立 | 均等 |
| `accuracy_truthfulness` | 信息准确性 | 事实正确、无虚假承诺、谨慎表述 | 均等 |
| `tone_empathy` | 语气共情度 | 语气亲和、情感共鸣、换位思考 | 均等 |

### 4.2 评分标准（1-5 分制）

| 分数 | 等级 | 描述 |
|------|------|------|
| **5** | 优秀 | 完美无问题，可作为标杆 |
| **4** | 良好 | 仅有细微瑕疵，整体优秀 |
| **3** | 一般 | 有待改进，存在轻微问题 |
| **2** | 较差 | 问题明显，部分检查清单命中 |
| **1** | 严重 | 问题严重，多项检查清单命中 |

### 4.3 深度分析输出

| 字段 | 说明 |
|------|------|
| `root_cause` | 根本原因分析 |
| `traced_node_id` | 溯源节点 ID |
| `traced_node_title` | 溯源节点名称 |
| `prompt_issue` | Prompt 问题描述 |
| `modification_suggestion` | 具体修改建议 |

---

## 五、数据格式要求

### 5.1 对话日志 (JSON)

```json
[
  {
    "session_id": "consult_roleplay_001",
    "domain": "医美咨询",
    "messages": [
      {"role": "user", "content": "你好，想咨询一下"},
      {"role": "assistant", "content": "您好！很高兴为您服务..."},
      {"role": "user", "content": "想了解瘦脸针"},
      {"role": "assistant", "content": "瘦脸针主要是..."}
    ]
  },
  {
    "session_id": "consult_002",
    "domain": "皮肤护理",
    "messages": [...]
  }
]
```

**字段规范**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `session_id` | string | ✅ | 会话唯一标识 |
| `domain` | string | ❌ | 业务领域，用于评测上下文 |
| `messages` | array | ✅ | 消息列表 |
| `messages[].role` | string | ✅ | `user` 或 `assistant` |
| `messages[].content` | string | ✅ | 消息内容 |

### 5.2 评分标准 (rubric.json)

```json
{
  "rubrics": [
    {
      "name": "clarity_sentence_structure",
      "description": "评估回复的表达清晰度",
      "criteria": {
        "5": "语句通顺流畅，结构清晰，逻辑严密",
        "4": "整体清晰，偶有瑕疵",
        "3": "基本可理解，存在表达问题",
        "2": "表达混乱，理解困难",
        "1": "语句不通，无法理解"
      },
      "low_score_checklist": [
        "语句不通顺或有语病",
        "表达含糊不清",
        "逻辑跳跃难以理解",
        "信息组织混乱"
      ]
    }
  ],
  "low_score_threshold": 3
}
```

**字段规范**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `rubrics` | array | ✅ | 评分维度列表 |
| `rubrics[].name` | string | ✅ | 维度代码（英文） |
| `rubrics[].description` | string | ✅ | 维度说明 |
| `rubrics[].criteria` | object | ✅ | 各分数档位描述 |
| `rubrics[].low_score_checklist` | array | ❌ | 低分检查清单 |
| `low_score_threshold` | int | ❌ | 低分阈值，默认 3 |

### 5.3 工作流配置 (Dify YML)

```yaml
app:
  name: 咨询助手工作流
  description: 医美咨询场景的智能客服
  version: 1.0.0

nodes:
  - id: llm_greeting
    type: llm
    title: 开场白
    prompt_template: |
      你是一位专业的医美咨询顾问。
      请用亲切的语气向用户问好并询问需求。
    
  - id: llm_consult
    type: llm
    title: 需求咨询
    prompt_template: |
      根据用户回复，深入了解其需求。
      注意：不要给出具体价格承诺。
```

---

## 六、输出格式

### 6.1 评测结果 (JSON)

```json
{
  "session_id": "consult_001",
  "evaluations": [
    {
      "turn_index": 1,
      "scores": {
        "clarity_sentence_structure": 4,
        "proactivity_interaction": 3,
        "content_benefits": 4,
        "persona_authority": 5,
        "accuracy_truthfulness": 4,
        "tone_empathy": 3
      },
      "avg_score": 3.83,
      "min_score": 3,
      "combined_score": 3.83,
      "overall_analysis": "该回复整体表现良好...",
      "target_response": "您好！瘦脸针是..."
    }
  ],
  "low_score_analyses": [
    {
      "turn_index": 5,
      "combined_score": 2.5,
      "root_cause": "Prompt 缺少输出长度约束...",
      "traced_node_title": "轻科普节点",
      "modification_suggestion": "在 Prompt 中增加..."
    }
  ]
}
```

### 6.2 数据库表结构

```sql
-- 评测批次
eval_batches (
    id, created_at, session_count, turn_count, 
    avg_score, low_score_count, 
    workflow_file, rubric_file, log_file
)

-- 会话结果
session_results (
    id, batch_id, session_id, domain,
    avg_score, turn_count, low_score_count
)

-- Turn 评测
turn_evaluations (
    id, session_result_id, turn_index,
    avg_score, min_score, combined_score,
    overall_analysis, scores_json
)

-- 低分分析
low_score_analyses (
    id, turn_evaluation_id,
    root_cause, traced_node_id, traced_node_title,
    prompt_issue, modification_suggestion
)
```

---

## 七、使用建议

### 7.1 评分标准优化

- 根据业务场景调整维度权重
- 低分检查清单应具体、可检验
- 各档位描述应清晰区分

### 7.2 评测效率

- 合并评测已优化为 1 次调用/回复
- 长对话建议分批处理
- 深度分析仅在低分时触发

### 7.3 结果分析

- 重点关注综合分，而非单纯平均分
- 查看最低分维度，识别短板
- 利用节点溯源定位 Prompt 问题

---

## 八、技术栈

| 组件 | 技术 |
|------|------|
| 前端框架 | Streamlit |
| 可视化 | Plotly |
| 数据库 | SQLite |
| LLM 接口 | OpenAI API |
| 配置格式 | JSON / YAML |

---

## 附录：更新日志

### v3.0 (2024-12-18)
- ✨ 合并评测：效率提升 6 倍
- ⚖️ 综合分算法：最低分惩罚机制
- 📚 历史评测：SQLite 持久化
- 🔧 低分阈值可选 1-4

### v2.0
- 两阶段评测
- 工作流节点溯源
- 报告导出

### v1.0
- 基础评测功能
- 多维度打分

---

*AI 对话评测系统 Pro v3.0 | 项目报告 | 2024-12-18*
